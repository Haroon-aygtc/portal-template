/* Department Section */
.department-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    margin-top: 20px;
}
.department {
    padding: 100px;
    display: flex;
    align-items: center;
    background: var(--section-bg);
}


.department-card {
    padding: 20px;
    background: var(--card-bg);
    border-radius: 10px;
    min-height: 160px;
    display: flex;
    align-items: flex-start;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    transition: transform 0.3s ease, box-shadow 0.3s ease, border-bottom 0.3s ease;
    border-bottom: 3px solid transparent;
    cursor: pointer;
    overflow: hidden;
    gap: 20px;
}

.department-card {
    flex: 0 0 100px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.department-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 25px rgba(0,0,0,0.08);
    border-bottom: 3px solid var(--primary-color);
}

.department-card .department-icon{
    width: 120px;
    min-width: 120px;
}

.department-card .department-icon img {
    width: 120px;
    min-width: 120px;
    height: 120px;
    object-fit: contain;
    transition: transform 0.3s ease;
}

.department-card:hover .department-icon img {
    transform: scale(1.1);
}

.department-card .service-content {
    flex: 1;
    overflow: hidden;
}

.department-card .service-content h5 {
    margin-bottom: 8px;
    font-size: 1.1rem;
    color: var(--text-color);
}

.department-card .service-content p {
    color: var(--muted-text);
    font-size: 0.9rem;
    margin-bottom: 10px;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.btn-service {
    color: var(--primary-color);
    text-decoration: none;
    font-size: 0.9rem;
    display: inline-flex;
    align-items: center;
    transition: color 0.3s ease;
}

.btn-service i {
    margin-left: 8px;
    transition: transform 0.3s ease;
}

.btn-service:hover i {
    transform: translateX(5px);
}

/* Responsive */
@media (max-width: 1200px) {
    .department-grid {
        grid-template-columns: repeat(2, 1fr);
    }
}

@media (max-width: 768px) {
    .department-grid {
        grid-template-columns: 1fr;
    }

    .department-card {
        padding: 20px;
    }

    .department-card .department-icon {
        width: 100px;
        min-width: 100px;
    }

    .department-card .department-icon {
        height: 150px;
    }
}